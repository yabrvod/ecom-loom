# Copiar a .env y completar con valores reales. NUNCA subir .env al repo.

# Base de datos
DB_URL=jdbc:postgresql://localhost:5433/ecommercedb
DB_USER=ecommerceuser
DB_PASSWORD=cambiar-por-password-seguro

# Redis
REDIS_HOST=localhost
REDIS_PORT=6380

# Spring Security (generado con: openssl rand -base64 32)
# No requerido explícitamente pero documentado aquí para referencia
